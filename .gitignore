# build output
build/
.gradle/
.apexcache/

# local config (org alias) & scratch — never canonical
alloyx.json
playground/

# internal planning doc — kept local, not published
PLAN.md

# IDE / class files
.idea/
*.class
